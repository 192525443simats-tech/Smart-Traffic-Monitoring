services:
  backend:
    build: .
    container_name: smart-traffic-backend
    ports:
  
        - "3000:5000"
    volumes:
      - traffic_database:/app/database
    restart: unless-stopped

volumes:
  traffic_database: